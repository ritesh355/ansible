- name: Install apache2 on Ubuntu node
  hosts: ubuntu-node
  become: yes
  tasks:
    - name: Install apache2
      apt:
        name: apache2
        state: present
        update_cache: yes
    - name: Start and enable apache2
      service:
        name: apache2
        state: started
        enabled: yes

